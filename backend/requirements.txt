# SENTIO - Backend Dependencies
# Python 3.10+ 필요

# === 웹 프레임워크 ===
fastapi>=0.104.0,<1.0
uvicorn[standard]>=0.24.0,<1.0
python-multipart>=0.0.6

# === AI / 영상 처리 ===
mediapipe>=0.10.9  # opencv-contrib-python, numpy 자동 설치됨
numpy>=1.26.0
ultralytics>=8.3.0  # YOLO11 다중 인원 감지

# === ONNX 런타임 (GRU 낙상 분류기) ===
# CPU 전용: pip install onnxruntime
# GPU 가속: pip install onnxruntime-gpu (CUDA 11.x/12.x 필요)
onnxruntime>=1.17.0  # CPU 기본

# === 데이터 검증 / 설정 ===
pydantic>=2.5.0,<3.0
pydantic-settings>=2.1.0,<3.0
python-dotenv>=1.0.0

# === 데이터베이스 ===
sqlalchemy>=2.0.23,<3.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
alembic>=1.13.0

# === 실시간 통신 ===
websockets>=12.0

# === 푸시 알림 ===
pywebpush>=2.0.0
py-vapid>=1.9.0

# === 인증 ===
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1

# === 리포트/내보내기 ===
openpyxl>=3.1.0

# === 프로덕션 서버 ===
gunicorn>=21.2.0

# === 모니터링 ===
psutil>=5.9.0

# === 테스트 ===
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.25.0
